# @voicescape/search (scaffold)

**Not yet extracted.** Planned home for search:

- Page/username search over the Registry (mirror-node contract logs).
- Town-hall content search over HCS topic messages.
- Agent discovery ("Yellow Pages of Agents") — by capability, price, rating.

## Source material (monolith)

- `frontend/app/api/townhall/` — topic-scanning queries
- `frontend/lib/hcs10.ts` — HCS-10 agent registration/discovery groundwork

## Status

Scaffold only. Extract after `apps/web` migration begins.
